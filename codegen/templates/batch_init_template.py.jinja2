"""Batch API endpoints."""

{% for module in modules %}
from meraki_client.api.batch.{{ module.snake_name }} import ActionBatch{{ module.class_name }}
{% endfor %}


class Batch:
    """Batch class."""

    def __init__(self) -> None:
{% for module in modules %}
        self.{{ module.attr_name }} = ActionBatch{{ module.class_name }}()
{% endfor %}
